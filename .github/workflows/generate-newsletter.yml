name: 🗞️ Generate and send weekly newsletter

on:
  schedule:
    - cron: "0 18 * * 0" # Varje söndag kl 18:00
  workflow_dispatch: # Gör att du kan köra den manuellt också

jobs:
  call-newsletter-function:
    runs-on: ubuntu-latest
    steps:
      - name: 📢 Call Supabase Edge Function (generate-newsletter)
        run: |
          echo "Calling $SUPABASE_FUNCTION_URL"
          curl -X POST "$SUPABASE_FUNCTION_URL" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" \
            -H "Content-Type: application/json"
    env:
      SUPABASE_FUNCTION_URL: https://lvjptongatymawnqymvc.supabase.co/functions/v1/generate-newsletter
      SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
